# Changelog

## 1.17.0 - 2026-08-13

- 稳定文件稿一行横跨多次换人时，在逐词时间戳可完整还原原文的前提下按真实说话人换点拆行，避免整段文字被“最大重叠说话人”一次性吞并。
- 无完整逐词时间、热词改写后无法逐字对应或说话人证据不足时继续沿用整行归属，不用推测性拆分破坏稳定文字。

## 1.16.9 - 2026-08-13

- 修复结束会议的尾部竞态：尾段文件校准和说话人聚类开始后，上游晚到的 partial/SentenceEnd 不再重新落成重复实时草稿。
- 结束阶段同步取消上游重连、轮换与首结果检测计时器；停止前已冻结的草稿和文件窗口仍按原顺序完整排空，不影响录音封存。

## 1.16.8 - 2026-08-13

- 实时短句声纹不再提前占用正式说话人编号：界面仍即时显示“说话人 1”，但首个可靠的 45 秒/长发言声纹才建立正式轨道，避免开场短音频污染画像并把单人会议错误拆成“说话人 1/2”。

## 1.16.7 - 2026-08-13

- 修复说话人 embedding 成功返回后缺少 `normalizeVector` 导入、导致会议轨道无法确认的问题，并增加纯函数回归测试覆盖真实响应归一化。

## 1.16.6 - 2026-08-13

- 修复 CampPlus 无法下载应用回源 URL 时说话人分离静默为空的问题：网关会将同一滚动 WAV 上传到 AIT 临时文件服务并用可访问 URL 重试；临时文件由 AIT 过期清理。
- 修复稳定文件稿事务漏传 `audioPath`，确保稳定行保留窗口录音来源；说话人增强的独立超时由 20 秒调整为 30 秒，不阻塞稳定文本落库和会议结束。

## 1.16.5 - 2026-08-12

- 修复说话人分离/声纹直连绕过统一配置层、只读取 `process.env.AIT_API_KEY` 的问题；公司端通过平台 `config.json` 注入密钥时不再出现文件 ASR 正常但说话人链路误判无凭证。
- CampPlus 分离请求显式携带 `language=zh` 与 `enable_words=true`，并补充 Bella TraceID 采集；错误详情会脱敏 URL、签名和令牌后再记录。

## 1.16.4 - 2026-08-12

- 修复公司端只配置 `PUBLIC_BASE_URL` 时，超过 2MB 的滚动 WAV 在调用说话人分离模型前因缺少 `AIT_PUBLIC_BASE_URL` 被静默丢弃的问题；说话人回源默认复用应用公网地址，只有显式配置时才使用独立 AIT 回源域名。
- 说话人分离与声纹提取失败不再静默吞掉：日志明确区分无可用音频 URL、上游 HTTP/请求 ID、空响应和解析异常，便于判断失败发生在分离前还是声纹阶段。
- 直连说话人分离补齐超时中止，避免上游无响应时后台增强任务无限悬挂。

## 1.16.3 - 2026-08-12

- 累计快照中途回改多个同音词时，按同一句上游任务内的受限编辑距离定位已提交区间，只追加新增尾部；覆盖线上 1x 回放中长口语句大幅回改后整句重复的真实样本。

## 1.16.2 - 2026-08-12

- 修复火山 LM 累计 partial 在浏览器 VAD 短暂停顿后被反复从句首落库的问题：VAD endpoint 不再重置累计游标，只提交尚未进入时间轴的尾部增量。
- SentenceEnd 会先扣除已经由 partial-progress 落轴的前缀，避免同一句最终稿再次形成一条完整重复草稿；真实的新句因前缀不匹配仍完整保留。
- 新增跨 VAD 端点累计快照、标点轻微变化及真实新句不误删回归。

## 1.16.1 - 2026-08-12

- 修复 45 秒说话人分离中“单次连续长发言只有一个 segment 时永远不能晋升”的问题：多段累计 4 秒或单段连续 6 秒均视为强证据，可建立新的会议级说话人轨道。
- 单段不足 6 秒仍只进入隐藏候选，不增加用户可见人数，继续防止咳嗽、回声和短噪声制造伪说话人。
- 稳定稿说话人增强失败现在记录明确原因（分离为空、轨道未确认、时间无重叠等），避免线上只有“待识别”却无诊断信息。

## 1.16.0 - 2026-08-11

- 新增会议级唯一说话人轨道管理器：实时声纹、45 秒文件分离和会后聚类都只提交观察证据，不再各自创建互不相认的 `说话人 N`。
- 实时草稿立即沿用当前暂定轨道（首条为 `说话人 1`），声纹异步返回后按同一 transcript id 原位纠正；人工编辑与人工说话人不被自动任务覆盖。
- 短噪声、模糊声纹只进入隐藏候选，不增加用户可见人数；同人被文件分离模型拆成两条高相似局部轨道时复用同一会议身份，避免说话人数膨胀。
- 45 秒稳定稿与会后聚类均复用会议轨道；声纹网关不可用时只保留暂定归属，不再启用第二套本地特征编号器。
- 新增即时草稿、异步原位纠正、跨窗口临时编号互换、同人拆轨合并及模糊证据不建新人等双端共享回归。

## 1.15.3 - 2026-08-10

- 修复文件 ASR 成功但仅返回全文 `text`、没有可用句级时间戳时，滚动稳定稿被错误判为“无 canonical 段”的断点。现在会以文件稿文字为事实、复用已落库实时 VAD 行的绝对时间轴受控分配，仍按同一事务替换草稿。
- 此降级路径会显式记录为 `file_text_timing_fallback`，保留已有说话人提示，并只做相邻窗口的连续前后缀去重；没有文件文字时不会把草稿伪装成稳定稿。
- 新增 text-only 文件 ASR、跨窗口连续边界去重回归；同时修复 core 仓库中相关测试的模块解析路径。

## 1.15.2 - 2026-08-09

- 修复公司端稳定稿说话人回填：CampPlus 回源读取滚动 WAV 时不再依赖浏览器 CAS Cookie，而使用仅绑定“会议 ID + 临时 rolling 文件名”的 5 分钟签名 URL。
- 签名只能读取当前滚动窗口音频，不能访问完整会议录音；正常用户回放仍保持原有会议权限校验。
- 公网端未配置签名密钥时维持原有公开音频 URL 行为，避免本次公司端权限修复影响公网端较长窗口的说话人分离。

## 1.15.0 - 2026-08-07

- 实时 partial 仅保留在顶部当前句气泡；会议列表只消费服务端已持久化的 `draft`/`stable` 行，删除浏览器临时行与文本模糊替换逻辑，杜绝同一时间片的重复、错误置顶和 30 分钟残留草稿。
- 已确认 `SentenceEnd` 的草稿路径改为直接、确定性落库：词库异步预热，逐句 LLM、实时声纹、diarization 与分段 WAV 均移出用户可见关键路径；新增 `draft_persisted` 延迟日志，便于按真实落库延迟排查。
- 对持续不发 `SentenceEnd` 的流式模型，partial 前缀持久化间隔从 12 秒收紧至 5 秒；两端配置一致且可用环境变量调节。

## 1.14.4 - 2026-08-07

- 流式草稿、45 秒文件校准与尾段补跑统一使用同一份有界 ASR 热词：按既有词库优先级去重，并统一限制为最多 80 个、400 个字符。
- 草稿元数据直接记录实际发送给 ASR 的热词列表，不再从原始词库重新展开别名，避免界面、审计记录与模型输入不一致。
- 新增热词数量、字符预算与跳过超额词后继续选择后续候选的共享回归测试。

## 1.14.3 - 2026-08-07

- 稳定文件稿以中心区间的左闭右开时间所有权落库；右边界片段会被丢弃而非写成 0ms 转写行，SQLite/MySQL adapter 也保留同一层输入防线。
- 尾段收口只可将未被文件稳定稿覆盖的草稿标记为 `realtime_fallback`；与任何非 fallback 稳定稿重叠的草稿会先软删除，避免“文件稿已到、尾段又将旧草稿升格”造成重叠和回放漂移。
- 新增共享中心边界回归与公网真实 SQLite 尾段回归，覆盖零时长保护及文件稿优先级。

## 1.14.2 - 2026-08-06

- 尾段文件校准递归、会中队列转尾段及失败重试统一继承同一个封存 `AbortSignal`；70 秒 deadline 到达后，迟到的文件 ASR 结果会在任何审计、删除或插入前被丢弃，不能再与实时稿兜底双写。
- 新增回归：文件 ASR 在 deadline 后才返回成功时，稳定稿存储写入必须为 0；已启动会中窗口在结束后继续处理的尾窗必须继承封存 deadline。

## 1.14.1 - 2026-08-06

- 修复结束会议与会中滚动文件 ASR 并发时的尾段收口竞态：封存会在 70 秒绝对期限内等待正在执行的窗口及其递归尾窗排空；只有无可提交中心区间、超时或失败时才将残留实时稿标记为 `realtime_fallback`。禁止文件稿在草稿兜底后再写入同一时间区间。
- 新增会话级回归：结束信号落在滚动文件 ASR 执行期间时，实时稿兜底必须严格发生在全部尾窗文件校准之后。

## 1.14.0 - 2026-08-06

- 滚动文件 ASR 只部分对齐实时草稿时，改为以中心窗口的文件稿整体替换未人工编辑自动行，杜绝未映射残片在封存时以 fallback 与稳定稿重复并存。
- 仅将具有 `fileSegmentCount > 0` 的对齐项视为真实文件稿映射，修复占位对齐行被错误算作已校准的问题。
- 整体替换会按绝对时间轴继承现有说话人提示，避免稳定稿因文本校准退化为“待识别”。
- 新增服务级回归测试，覆盖部分对齐整体替换、人工行保护路径与说话人提示继承。

## Unreleased

### Added

- 新增完整项目文档入口、当前状态、双端前端同步、公司测试环境部署和质量回归口径。
- 共享前端同步范围扩展到统一 `App.tsx`，公司版登录后页面与公网版使用同一套流程。

### Changed

- 文档以完整录音、绝对时间轴、45 秒稳定文件 ASR、稳定转写合成、异步说话人回填和证据化纪要为当前基线。
- 公网与公司版不再依赖 git submodule 或开发机符号链接；云构建读取仓库内真实文件。

## v1.7.4 - 2026-07-29

### Added

- **P0 实时会议链路正确性**：
  - flush 时间冻结（v1.7.0）：`enqueueFlush` 快照加 `capturedAtAudioMs`，缺少 ASR 句尾时间戳时只能用快照的 `capturedAtAudioMs`（不是排队执行时的当前会议时间），防止前一段耗时较长时扩大前一段时间区间。
  - AudioWorklet 资源路径用 `import.meta.env.BASE_URL`（不硬编码根路径）+ VAD 下沉到 AudioWorklet（主线程不重复 PCM→Float32 转换）+ 资源释放关闭 Worklet port。
  - 稳定稿失败可观测性（v1.7.1）：rolling 文件 ASR 失败/LLM 对齐失败/队列满分别有结构化状态码和原因；尾段降级发送 `tailFailureCode`/`tailFailureReason`/`tailFailedWindows`（服务端可查询失败原因和重试次数）。

- **P1 双端 Adapter 收敛**：
  - 公司端 23 个内联函数迁入 `company-adapter.mjs`，公网端 20 个内联函数迁入 `public-adapter.mjs`。
  - core 只依赖接口（`deps.xxx`），不知道 MySQL/SQLite/CAS/Gateway 实现细节。
  - 契约测试不依赖真实服务/MySQL（`shared-routes-registered` 改为 fake Adapter 验证路由注册）。
  - 全量测试：公司端 86/86 全绿（1 秒）、公网端 91/91 全绿（1 秒）。

- **P2 同步门禁强化**：
  - 双端 CI 门禁：`frontend-sync`、`core-sync`、`injection`、`api-contract`。
  - 干净 checkout 校验（GitLab `GIT_DEPTH=0` 完整克隆，GitHub tarball 不用 `git clone` CDN 缓存）。
  - 只改清单不改文件必然失败、只改一端共享文件必然失败（哈希校验）。

- **P3 双端行为回归**：
  - `dual-end-regression.test.mjs`：单人长讲述/多人交替与抢话/弱网重连与停止，验证时间轴单调性、稳定稿覆盖率、说话人轨道（两端跑同一输入，输出必须一致）。
  - 测试动态检测 modules 位置（core 里 `../modules/`，消费端 `./`）。

- **P4 部署链路与版本溯源闭环**：
  - build-info 五版本字段：`appCommit`、`buildTime`、`coreVersion`、`frontendRevision`、`deploymentProfile`。
  - health 返回与构建日志一致（`gen-build-info.mjs` 打印 `build-info.json written: xxx core=xxx frontend=xxx`）。
  - 域名访问的 health 与容器内 health 一致（`getBuildInfo()` 不缓存，每次读 `build-info.json`）。

- **Adapter 契约测试**（覆盖 Codex 建议的 5 个维度）：
  - 公司端 18 个测试（`company-adapter.test.mjs`）、公网端 16 个测试（`public-adapter.test.mjs`）。
  - 输入/输出与错误语义、事务与幂等性、多租户/权限边界、SQLite/MySQL 行为差异、降级策略。

- **发布前环境冒烟**（`smoke-company-db.mjs`）：
  - 连真实公司测试库验证连接/迁移/关键会议接口，失败时明确标记环境/连接失败或代码失败。
  - 读写探针用事务（finally 中回滚，不残留测试数据）。
  - 应用级冒烟（`/api/health` 验证 HTTP 路由、鉴权、配置装配）。

### Changed

- **核心原则**：Adapter 管能力差异，不管 HTTP 路由——`index.mjs` 可以很薄，但仍负责"把共享路由挂到 HTTP 服务上"。
- **分层架构**：core（会议业务、共享路由处理器、接口契约、回归测试）→ adapter（DB/存储/AI/用户上下文的差异实现）→ shell（HTTP 服务启动、CAS 或公网鉴权挂载、环境变量读取）。

### Fixed

- **CI 文案统一**：数据库不可用时失败并标记基础设施问题，不静默通过。
- **smoke-company-db 从自动 CI 移除**：加单独的手动触发的 `company_db_smoke` job（生产环境部署前跑，CI 里没有真实公司测试库）。
- **CI 工作目录风险**：`cd server && node smoke-company-db.mjs` 改为 `(cd server && node smoke-company-db.mjs)`（子 shell，不影响后续 `npm run build`）。
- **`./shared/` → `./` 转换**：直接用 GitHub API 更新文件不会跑 `sync-frontends.mjs` 的 `./shared/` → `./` 转换——必须手动转换或触发 CI 的 sync 步骤。
- **CI 的 `git clone` 命中 GitHub CDN 缓存**：改用 `gh api` 下载 tarball（强制刷新）。
- **`formatAudioOffset` 移到 `live-asr-helpers.mjs`**：Adapter 从 `live-asr-helpers` import（不是 `index.mjs`，避免循环依赖）。

## v0.1.0 - 2026-06-24

### Added

- 完成 MVP 主链路：项目、会议、录音、实时转写、AI 实施总结、待办池、历史会议、项目记忆和项目 AI 对话。
- 支持真实麦克风 ASR、说话人识别、人工说话人修正和转写导出。
- 支持会后最终纪要、历史会议详情、Markdown 导出和时间轴/金句定位转写。
- 支持项目记忆结构化沉淀和人工编辑。
- 支持公网服务通过内网 AI Gateway 调用企业内网模型能力。
- 补全文档体系：产品、功能、技术架构、路线图、开发环境、Git 协作、发布流程和版本管理。

### Changed

- 录音状态改为跨页面保持，切换导航不主动停止录音。
- 实时会议导航在录音中显示红色录音标识。
- ASR WebSocket 意外断开后自动重连。

### Known Issues

- 长会议稳定性仍需要 60/120 分钟真实压测。
- 会后编辑器仍需继续优化结构化解析和右侧待办/风险同步。
- 项目 AI 对话目前只回答问题，尚未支持一键沉淀为项目记忆或待办。
