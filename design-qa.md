# Design QA

最后更新：2026-07-14

## 当前结论

产品已从“页面闭环”进入“真实会议体验与质量闭环”阶段。设计验收的首要标准不再是单个卡片是否完整，而是用户能否在开会、回听、修正、归档四个连续动作中不丢失上下文。

## 已确认体验

- 实时录音跨页面保持；录音中导航有可见状态，不要求用户停留在会议页。
- 实时 ASR 的每个确认句段立即进入时间轴，稳定稿随后在原位置更新，避免产生“中间内容被吞掉”的感受。
- 历史会议使用完整源音频回放；拖动播放、章节跳转和文字定位共用绝对时间轴。
- 播放时当前文字和说话人应同时跟随；转写和说话人允许后续编辑。
- 项目页、待办、历史会议和项目记忆均按项目隔离；会中不会误加载其他项目的上下文。
- 热词、修词、待办状态、人工编辑和说话人命名都有明确人工优先级。
- 公司版登录后页面与公网版使用同一套共享 `App.tsx` 和组件；CAS 只影响登录前入口。

## 关键设计规则

1. 预览、稳定、编辑三种文字状态必须可区分，但不得让稳定更新造成列表跳跃或消失。
2. 所有时间相关交互以音频时间轴为准，不能用列表索引、临时窗口序号或估算时长定位。
3. 将“讨论、建议、决策、待确认待办”视觉和文案区分，避免 AI 结果显得过度确定。
4. 播放器和转写列表的主操作应始终可见；长页面中的 tab、播放器控制和当前焦点不可因滚动失去上下文。
5. 错误与降级应提示事实：例如“稳定校准暂未完成，已保留实时草稿”，而不是把未完成表现成成功。

## 仍需回归

- 长会议持续播放时的自动滚动、手动滚动锁定和当前行高亮。
- 多人重叠发言时说话人呈现与转写分行是否造成误导。
- 稳定稿替换后，章节、待办证据和回放定位是否仍指向正确时间。
- 公司 CAS 登录后的首次加载、缓存更新与新版构建资源是否一致。
- 小屏幕下历史播放与编辑面板的可用性。

详细产品验收规则见 [功能规格](docs/product/FEATURE_SPEC.md)，质量验收见 [ASR 基准](docs/quality/ASR_BENCHMARK.md)。
